GAME         := tron
SRC_DIR      := vhdl_tron_rev_0_3_2019_11_22
TOP_ENTITY   := tron_basys3
IO_SUMMARY   := PS/2 keyboard (JB) + JA Atari-style joystick OR-merged (spinner keyboard-only); btnC=reset; JC PmodAMP2 PWM audio (left channel); sw(14)=amp shutdown, sw(15)=amp gain; no dip switches
DISPLAY_HINT := F8 toggles 31 kHz VGA / 15 kHz TV

include ../tools/mk/machine.mk
