GAME         := kick
SRC_DIR      := vhdl_kick_rev_0_2_2019_11_22
TOP_ENTITY   := kick_basys3
DISPLAY_NAME := Kick
IO_SUMMARY   := PS/2 keyboard (JB); JA joystick (active-low); btnU=coin1, btnD=coin2; btnL=start1, btnR=start2; JC PmodAMP2 PWM audio; sw(14)=shutdown, sw(15)=gain; VGA 4-4-4 RGB
DISPLAY_HINT := F8 toggles 31 kHz VGA / 15 kHz TV

include ../tools/mk/machine.mk
